# Copyright (C) 2012 Codership Oy
# This config file is to be sourced by garb service script.

# THIS FILE IS AUTO GENERATED BY PUPPET.

# A comma-separated list of node addresses (address[:port]) in the cluster
<% if @galera_nodes.class == 'Array' -%>
GALERA_NODES="<%= @galera_nodes.join(',') -%>"
<% else -%>
GALERA_NODES="<%= @galera_nodes -%>"
<% end -%>

# Galera cluster name, should be the same as on the rest of the nodes.
GALERA_GROUP="<%= @galera_group -%>"

# Optional Galera internal options string (e.g. SSL settings)
# see http://galeracluster.com/documentation-webpages/galeraparameters.html
<% if @galera_options != nil -%>
GALERA_OPTIONS="<%= @galera_options -%>"
<% end -%>

# Log file for garbd. Optional, by default logs to syslog
# Deprecated for CentOS7, use journalctl to query the log for garbd
<% if @galera_logfile != nil -%>
LOG_FILE="<%= @galera_logfile -%>"
<% end -%>
